import { Component, OnInit,Input,OnChanges } from '@angular/core';
import {
    trigger,
    state,
    style,
    animate,
    transition
  } from '@angular/animations';
@Component({
  selector: 'app-list-bar-char',
  templateUrl: './list-bar-char.component.html',
  styleUrls: ['./list-bar-char.component.css'],
//   animations: [
//     trigger('flyInOut', [
//       state('in', style({transform: 'translateX(0)'})),
//       transition('void => *', [
//         style({transform: 'translateX(-100%)'}),
//         animate(100)
//       ]),
//       transition('* => void', [
//         animate(100, style({transform: 'translateX(100%)'}))
//       ])
//     ])
//   ]
})
export class ListBarCharComponent implements OnInit {

  @Input()
  public optionData; //只关注数据层面

  constructor() { }

  ngOnInit() {
  }
  
  OnChanges(){
    //   console.log(this.optionData);
    //   this.optionData = this.optionData;
  }

 
}
