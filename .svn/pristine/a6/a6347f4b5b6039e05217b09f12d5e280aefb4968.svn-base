import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListBarVerticalCharComponent } from './list-bar-vertical-char.component';

describe('ListBarVerticalCharComponent', () => {
  let component: ListBarVerticalCharComponent;
  let fixture: ComponentFixture<ListBarVerticalCharComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListBarVerticalCharComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListBarVerticalCharComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
