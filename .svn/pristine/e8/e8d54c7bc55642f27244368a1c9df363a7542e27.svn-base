import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListBarCharComponent } from './list-bar-char.component';

describe('ListBarCharComponent', () => {
  let component: ListBarCharComponent;
  let fixture: ComponentFixture<ListBarCharComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListBarCharComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListBarCharComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
